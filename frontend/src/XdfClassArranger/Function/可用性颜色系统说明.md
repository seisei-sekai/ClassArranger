# 可用性颜色系统说明

## 问题背景

当系统中有大量学生（如44个）时，单一灰色系的透明度变化无法提供足够的视觉区分度，用户难以在概览图中快速识别不同区域的学生可用性差异。

## 解决方案

采用**日系传统渐变色彩系统**，根据可用学生比例从冷色调（少学生）过渡到暖色调（多学生），提供清晰的视觉对比。

---

## 色彩方案

### 🔵 浅葱色 (Asagi) - 0-20% 学生可用
- **RGB**: `(132, 169, 169)`
- **透明度**: 0.3 - 0.6
- **含义**: 很少学生可用，冷静的淡青蓝色
- **场景**: 大部分学生不可用的时段

### 🟢 若竹色 (Wakatake) - 20-40% 学生可用
- **RGB**: `(104, 155, 137)`
- **透明度**: 0.4 - 0.6
- **含义**: 少量学生可用，清新的青绿色
- **场景**: 部分学生可用，但选择有限

### 🟡 若草色 (Wakakusa) - 40-60% 学生可用
- **RGB**: `(136, 153, 99)`
- **透明度**: 0.5 - 0.7
- **含义**: 中等数量学生可用，温和的黄绿色
- **场景**: 约一半学生可用，选择适中

### 🟠 柑子色 (Kouji) - 60-80% 学生可用
- **RGB**: `(183, 143, 93)`
- **透明度**: 0.6 - 0.75
- **含义**: 较多学生可用，温暖的橙黄色
- **场景**: 大部分学生可用，选择丰富

### 🔴 紅梅色 (Koubai) - 80-100% 学生可用
- **RGB**: `(170, 109, 91)`
- **透明度**: 0.7 - 0.9
- **含义**: 绝大多数学生可用，醒目的橙红色
- **场景**: 几乎所有学生都可用，最佳排课时段

---

## 视觉优势

### 1. **高对比度**
- 从冷色（蓝绿）到暖色（橙红）的渐变，提供强烈的视觉差异
- 即使有44个学生，不同比例区间也能轻松区分

### 2. **直觉理解**
- 冷色 = 少/不好（类似"冷门时段"）
- 暖色 = 多/好（类似"热门时段"）
- 符合人类的自然色彩感知

### 3. **日系美学**
- 使用传统日本色彩，与整体UI风格一致
- 柔和而不刺眼，长时间使用不疲劳

### 4. **渐进透明度**
- 除了颜色变化，透明度也随比例增加
- 双重视觉提示，更容易识别

---

## 使用方法

### 在日历上查看

1. **点击"可用时间"按钮**
   - 显示所有学生的时间可用性热图

2. **查看颜色图例**
   - 图例会自动出现在学生列表上方
   - 显示5个颜色档位及其对应的比例

3. **一眼识别最佳时段**
   - 橙红色区域 = 最佳排课时段（80%+学生可用）
   - 黄绿色区域 = 次优时段（40-60%学生可用）
   - 青蓝色区域 = 避开时段（0-20%学生可用）

### 点击查看详情

1. **点击任意时间块**
   - 右侧弹出详情面板
   - 显示该时段所有可用学生及其约束条件

2. **对比不同时段**
   - 通过颜色快速对比
   - 找到最多学生重叠的时段

---

## 技术实现

### 颜色计算函数

```javascript
const getAvailabilityColor = (ratio) => {
  if (ratio <= 0.2) {
    // 浅葱色 (淡青蓝)
    const intensity = ratio / 0.2;
    return `rgba(132, 169, 169, ${0.3 + intensity * 0.3})`;
  } else if (ratio <= 0.4) {
    // 若竹色 (青绿)
    const intensity = (ratio - 0.2) / 0.2;
    return `rgba(104, 155, 137, ${0.4 + intensity * 0.2})`;
  } else if (ratio <= 0.6) {
    // 若草色 (黄绿)
    const intensity = (ratio - 0.4) / 0.2;
    return `rgba(136, 153, 99, ${0.5 + intensity * 0.2})`;
  } else if (ratio <= 0.8) {
    // 柑子色 (橙黄)
    const intensity = (ratio - 0.6) / 0.2;
    return `rgba(183, 143, 93, ${0.6 + intensity * 0.15})`;
  } else {
    // 紅梅色 (橙红)
    const intensity = (ratio - 0.8) / 0.2;
    return `rgba(170, 109, 91, ${0.7 + intensity * 0.2})`;
  }
};
```

### 特性

- **平滑过渡**: 每个区间内透明度线性插值
- **边界清晰**: 20%为间隔的5个档位，易于识别
- **性能优化**: 颜色预计算，不影响渲染性能

---

## 扩展性

### 如果需要更多档位

可以轻松添加更细的颜色分级：

```javascript
// 例如：10个档位，每10%一个颜色
if (ratio <= 0.1) { /* 极少 */ }
else if (ratio <= 0.2) { /* 很少 */ }
else if (ratio <= 0.3) { /* 少 */ }
// ... 以此类推
```

### 如果需要其他配色方案

只需修改RGB值，保持结构不变：

```javascript
// 例如：使用蓝-紫-红渐变
if (ratio <= 0.2) {
  return `rgba(100, 150, 255, ${...})`; // 蓝色
} else if (ratio <= 0.4) {
  return `rgba(150, 120, 255, ${...})`; // 紫蓝
}
// ...
```

---

## 对比效果

### 旧方案（单一灰色）
```
学生数量  1   5   10   20   30   44
颜色    ■   ■   ■    ■    ■    ■
       (几乎无法区分，特别是20+学生时)
```

### 新方案（渐变色系）
```
学生数量  1   5   10   20   30   44
比例    2%  11%  23%  45%  68%  100%
颜色    🔵  🟢  🟢   🟡   🟠   🔴
       (清晰可辨，一眼就能看出差异)
```

---

## 最佳实践

### 1. 排课策略
- **优先选择橙红色时段**: 学生选择最多，排课成功率高
- **避开青蓝色时段**: 学生选择少，可能无法满足需求
- **黄绿色时段作为备选**: 适合小班或1对1排课

### 2. 数据分析
- **识别热门时段**: 大片橙红色区域 = 普遍受欢迎的时段
- **发现冷门时段**: 大片青蓝色区域 = 需要引导调整的时段
- **平衡课程分布**: 避免所有课程集中在橙红色时段

### 3. 与学生沟通
- **可视化时间冲突**: 用颜色图直观展示为什么某些时段难以排课
- **引导时间选择**: 建议学生考虑黄绿色或橙色时段

---

## 注意事项

⚠️ **色盲友好性**: 当前配色主要依赖色相变化，建议后续可添加：
- 图案/纹理辅助（如斜线密度）
- 可选的高对比度模式
- 数字标签显示（如"15/44"）

⚠️ **打印友好性**: 如需打印，建议添加打印样式：
- 转换为灰度渐变
- 添加边框或图案区分

---

## 总结

通过引入日系传统渐变色彩系统，成功解决了大量学生时可用性难以区分的问题。新方案提供了：

✅ **高视觉区分度**: 即使44个学生也能清晰区分  
✅ **直觉化理解**: 冷暖色系符合自然认知  
✅ **美观舒适**: 日系配色柔和不刺眼  
✅ **易于使用**: 配合图例和详情面板，功能完整  

现在用户可以一眼识别最佳排课时段，大大提升了排课效率！

